const router = require('express').Router();

// import api routes here, change 'filename' to desired file.
// const filename = require('./filename');

// express api routes here, channge 'filename' to desired file.
// router.use('/filename', filename);

module.exports = router;
