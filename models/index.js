// grab exported models here:
// const example = require('./example');
// const other_example = require('./other_example');

// when a model is references another model:
// example.hasMany(other_example, { foreignKey: 'example_id', });
// other_example.belongsTo(example, { foreignKey: 'example_id', });

// export models here:
// module.exports = { example };